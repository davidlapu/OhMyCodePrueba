El proyecto usa la version 17 de Java

Dependencias de Angular

```
npm install -g @angular/cli 
npm install bootstrap jquery @popperjs/core
```

Para iniciar el servidor de Angular
```
cd .\angularclient\
ng serve --open 
```